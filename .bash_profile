source ~/.profile
source ~/.bashrc 
source ~/.alias

# reload profile
# source ~/.bash_profile

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

# git autocomplete
#if [ -f ~/.git-completion.bash ]; then
#	  . ~/.git-completion.bash
#fi

